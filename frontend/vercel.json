{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run prebuild && prisma generate --schema=./prisma/schema.prisma --generator=client && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"]
}
